apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pb-blueprint-ruby.fullname" . }}-test-connection"
  labels:
{{ include "pb-blueprint-ruby.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "pb-blueprint-ruby.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
